{
  "name": "brokerverse",
  "version": "1.0.0",
  "private": true,
  "description": "BrokerVerse - insurance broking core platform by iorta TechNXT",
  "workspaces": [
    "shared",
    "server",
    "web",
    "e2e"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c cyan,green \"npm run dev -w server\" \"npm run dev -w web\"",
    "build": "npm run build -w shared && npm run build -w server && npm run build -w web",
    "typecheck": "npm run typecheck -w shared && npm run typecheck -w server && npm run typecheck -w web",
    "lint": "npm run lint -w shared && npm run lint -w server && npm run lint -w web",
    "test": "npm run test -w shared && npm run test -w server && npm run test -w web",
    "test:e2e": "npm run test -w e2e",
    "db:migrate": "npm run db:migrate -w server",
    "db:seed": "npm run db:seed -w server",
    "db:reset": "npm run db:reset -w server",
    "quality": "node scripts/quality-gate.mjs",
    "coverage": "npm run coverage -w shared && npm run coverage -w server && npm run coverage -w web",
    "setup": "node scripts/setup-db.mjs && npm run db:reset",
    "start": "node server/dist/index.js"
  },
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "@vitest/coverage-v8": "^5.0.1",
    "concurrently": "^9.1.2",
    "esbuild": "^0.28.2",
    "eslint": "^9.17.0",
    "eslint-plugin-react-hooks": "^5.1.0",
    "eslint-plugin-sonarjs": "^4.2.1",
    "jscpd": "^5.3.1",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.18.2",
    "vitest": "^5.0.1"
  },
  "engines": {
    "node": ">=20"
  },
  "type": "module",
  "dependencies": {
    "dotenv": "^16.4.7",
    "pg": "^8.13.1"
  }
}
